name: java-template
services:
  app:
    cap_add:
      - SYS_ADMIN     # async-profiler perf events
    security_opt:
      - seccomp:unconfined
  flight:
    environment:
      FLIGHT_INTERVAL_MS: "3600000"  # idle stream so HTTP is the sole LSM writer
